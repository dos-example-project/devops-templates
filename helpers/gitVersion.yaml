### This is task to check lastest component version and bump to new version 
### New Version base on requierment and which release version tools.
### For ex: it can base on commit convention. (use tools semantic-release )